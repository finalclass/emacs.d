My private emacs configuration
=============================

To install type:

```bash
git clone git@github.com:finalclass/emacs.d.git ~/.emacs.d
cd ~/.emacs.d
git submodule update --init
echo "(load-file "~/.emacs.d/init.el")" >> ~/.emacs
```
